Claude, preciso da sua ajuda como especialista em UI/UX para React Native. Eu vou te enviar as imagens do design feito no Figma.

Seu objetivo é reescrever APENAS a parte visual (JSX e StyleSheet) para que o aplicativo fique idêntico ao design da imagem.

⚠️ REGRAS INVIOLÁVEIS (LEIA COM ATENÇÃO):
1. Respeite o Contexto: Lembre-se de todas as restrições da arquitetura, do fluxo de Oracle/Firebase e das regras da Sprint que te passei nos arquivos `.md` anteriores.
2. NÃO toque na lógica de negócio: É estritamente proibido alterar, remover ou renomear qualquer estado (`useState`), chamadas de API (`fetch`, Axios, TanStack Query), `useEffect` ou funções de manipulação de dados (`handle...`).
3. NÃO altere a navegação: Mantenha as chamadas do React Navigation exatamente como estão. 
4. NÃO invente bibliotecas visuais pesadas: Use os componentes nativos do React Native (`View`, `Text`, `TouchableOpacity`, `ScrollView`, etc.), Flexbox e o `StyleSheet`. Se precisar de ícones, use apenas o `@expo/vector-icons` que já está no projeto.

O que VOCÊ PODE e DEVE fazer:
- Extrair as cores, tamanhos de fonte, border-radius e espaçamentos (paddings/margins) da imagem enviada.
- Reestruturar a árvore do JSX (adicionar ou remover `Views` contêineres) exclusivamente para atingir o alinhamento correto com Flexbox.
- Deixar a tela responsiva e com um visual moderno e polido, condizente com a imagem.

Abaixo está as prints atuais da tela. Me devolva o arquivo completo atualizado, mantendo toda a lógica intacta e alterando apenas o design:

